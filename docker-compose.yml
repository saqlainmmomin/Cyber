services:
  api:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - OPENROUTER_KEY=${OPENROUTER_KEY}
      - DATABASE_URL=sqlite:///data/dpdpa.db
