# Phase 5 dispatch continuation: execute P5-2, P5-4, P5-6

**Read this first.** This is a continuation handoff, not a new task — a cloud session wrote the three remaining Phase 5 handoffs (step 1 of the process below) and could not go further, because it has no local filesystem for `git worktree`, no local `codex` CLI, and its network policy blocks PyPI (so it couldn't even run `pytest`). Your job is to run steps 2-9: dispatch to Codex, review, merge. This runs on the local machine (`/Users/saqlainmomin/dpdpa-gap-tool`) — it needs local filesystem access, `git worktree`, and the local `codex` CLI, exactly like Phases 1-4 and P5-1/P5-3/P5-5/P5-8 did. Do not run this from a cloud/remote session that lacks those.

## Goal

Get the last three Phase 5 tasks (P5-2 reader migration, P5-4 adaptive UCC questionnaire, P5-6 RFI rebuild) implemented by Codex, independently reviewed, and merged to `main`. Definition of done: three PRs merged, full test suite green on `main`, `tasks/todo.md` / `CLAUDE.md` / project auto-memory all updated to reflect Phase 5 complete (P5-7 stays reserved and unscheduled, per the plan).

## Current state

- `main` is at commit `d69aa71` (P5-8, P5-1, P5-5, P5-3 merged — PRs #38-#41). **The cloud session could not run `pytest` at all** (PyPI is blocked in its container), so there is no fresh baseline number to trust. The last recorded figure, from P5-3's own `## Results`, is **591 passed, 9 skipped**, and `tasks/todo.md` says 593 — **re-verify yourself** with `.venv/bin/pytest -q` in a clean worktree of `d69aa71` before dispatching anything. Every prior phase's continuation handoff says the same thing for the same reason (don't trust a stated number); this time it's not caution, the number was genuinely never measured.
- Three detailed implementation handoffs were written by isolated Opus subagents (each read the plan, the relevant merged handoffs' full `## Results` sections, and grounded its design against the actual current code by reading source directly — none could run tests) and are committed on branch `claude/peaceful-bardeen-0viroy`, three commits ahead of `main`:
  - `tasks/handoffs/2026-09-24-p5-2-reader-migration.md` — **21 decisions (D-P5-2-A through U). This is "the most important task in this phase" per the plan, and it needs a second, more skeptical review pass beyond the standard gate (like P2-3 and P4-4 got) — do not settle for one clean review on this task alone.** It moves scores, the report tab, the PDF and the release gate off AI-proposed `GapItem`s onto consultant-approved Conclusions (PR-045, PR-052, D3). Key design points to re-verify before dispatch: a new read-only module `app/services/approved_report.py` is the single source of truth for the approved view; `insufficient_evidence` is excluded from the scoring denominator but its count is always shown; the legacy bulk-approval routes (`approve_assessment`, `disposition_item`, `reject_assessment`) are retired to HTTP 410, not deleted; `GapReport` survives unchanged as the frozen AI-proposal record (not a cache); release becomes an explicit `POST /api/assessments/{id}/release` act bound to an exact manifest of Conclusion versions, so any later change to an in-scope Conclusion or scope automatically makes a prior release "stale". **It changes client-visible numbers for existing assessments** (issued snapshots stay immutable; live reports don't) — this is stated as the point of the task in the plan's own risk table, not a bug.
  - `tasks/handoffs/2026-09-24-p5-4-adaptive-ucc-questionnaire.md` — **13 decisions (D-P5-4-A through M).** Gives UCC cluster questions the same deepen/pre-fill/tier treatment DPDPA-only questionnaires already have, built on P5-3's `scoped_findings`/`load_desk_review_data` API. The cluster-status rule: a cluster question deepens if any member control (any framework) has an absence, `absent` coverage, or a signal; it pre-fills only if every member has adequate/partial coverage with a grounded quote and none is flagged; otherwise it stays active. Pre-fill rows are keyed by the cluster question id with the existing `"document"` answer source (no new source added, so P5-1's `UNCONFIRMED_ANSWER_SOURCES` gate covers them for free). Screening stays DPDPA-only, hidden elsewhere, with its unverified "~30%" claim removed from the copy. **Can run in parallel with P5-2** (see Constraints) but the writing agent flagged that `root_cause_clusters` and the actual cross-framework cluster map (`app/frameworks/mappings/clusters.py::CONTROL_CLUSTERS`) are two different things — the original plan brief conflated them; the handoff corrects this. It also found and fixes a real bug: `run_desk_review` only commits after pre-fill when a *new* row was created, so updates to an existing pre-fill are silently lost on both the DPDPA and cluster paths.
  - `tasks/handoffs/2026-09-24-p5-6-rfi-rebuild.md` — **15 decisions (D-P5-6-A through O), 22 test scenarios. Depends on P5-2 (design, not just code) and on P5-5 (merged).** Rebuilds the RFI as a new write-once `report_snapshots` type sourced from `approved_report.build_approved_report(...).rows` (P5-2) plus the merged evidence-request checklist (`compute_scope_multi(...)["evidence_checklist"]`, P5-5), retiring the unversioned `GapItem`-sourced `RFIDocument` (old routes → 410, `rfi_generator.py` deleted, `rfi_documents` table kept as read-only history). **Resolves an apparent conflict between P5-2's coordination note and the plan's own P5-6 scope**: P5-2's handoff says RFI downloads will "pass the new gate," but the plan explicitly says the RFI's "download no longer sits behind the full report release gate" because an evidence request necessarily precedes conclusions. This handoff sides with the plan — the RFI is gated only on scope being recorded and the engagement not being archived, never on report release — and adds a standing guard test that forbids any release-gate call anywhere in the new RFI code. It also closes P5-5's punted magic-link question (D-P5-5-J): `magic_links.scope_json` gets a version-2, reference-only shape (an RFI item id plus the assessment/snapshot id per link item) so requirement mappings are resolved from the immutable issued RFI version at read time rather than copied into the link; `MAX_ITEMS` (20) and the client-facing surface are unchanged, and a consultant can create multiple links to cover more than 20 items. **Because it was written before P5-2 was implemented, every P5-2 symbol it references (`approved_report.build_approved_report`, `ApprovedRow` fields, `release_state`) comes from P5-2's handoff text, not from merged code — its own Step 0 tells Codex to check those names against the actually-merged P5-2 code first and stop if anything differs.**
- The general dispatch process (worktree setup, Codex invocation, commit/review/merge mechanics) is documented in `tasks/handoffs/2026-09-24-phase-4-kickoff.md`, steps 2-9, and was followed as-is for P5-1/P5-3/P5-5/P5-8 — nothing about that mechanical process changes for these three tasks. Read it if you haven't run this loop recently.
- `docs/plans/2026-09-24-001-cleanup-and-non-dpdpa-parity-plan.md` is still the source-of-truth plan; nothing in it needs re-reading unless a handoff's own `## Decisions` section tells you to check a specific cross-reference.
- The plan's "Explicitly out of scope / deferred" table was reviewed against the now-merged P5-1/P5-3/P5-5 state (in this same cloud session, in chat, not written to a file). Conclusion: none of the 10 deferred items are worth pulling forward — their deferral reasons are content-authorship/licensing/product-sequencing, not engineering blockers these three merges remove. No action needed here; mentioned so you don't need to re-derive it.

## Key files

- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/handoffs/2026-09-24-phase-4-kickoff.md` — the process document (steps 2-9 are what you execute).
- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/handoffs/2026-09-24-p5-2-reader-migration.md` — P5-2 spec.
- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/handoffs/2026-09-24-p5-4-adaptive-ucc-questionnaire.md` — P5-4 spec.
- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/handoffs/2026-09-24-p5-6-rfi-rebuild.md` — P5-6 spec.
- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/handoffs/2026-09-24-p5-1-correctness-bundle.md`, `2026-09-24-p5-3-framework-aware-desk-review.md`, `2026-09-24-p5-5-framework-scoping-evidence.md` — the three merged Phase 5 handoffs, each with a `## Results` section the new tasks build on as a fixed contract. Re-read the `## Results` section of each before dispatching the task that depends on it.
- `/Users/saqlainmomin/dpdpa-gap-tool/docs/plans/2026-09-24-001-cleanup-and-non-dpdpa-parity-plan.md` — the plan.
- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/agent-ownership.md` — ownership criteria (all three tasks here are "Claude designs → Codex implements", consistent with the phase's other tasks).
- `/Users/saqlainmomin/dpdpa-gap-tool/CLAUDE.md` — "Current phase" line to update after merges.
- `/Users/saqlainmomin/dpdpa-gap-tool/tasks/todo.md` — task checklist to update after each merge.
- Project auto-memory (`project_status.md`, `MEMORY.md` in this project's memory directory) — update after merges.

## Sequencing

```
P5-2 (extra-skeptical review) ──┬── P5-6
P5-4 (parallel with P5-2) ──────┘
```

- **P5-2 gates P5-6.** P5-6's own handoff was written against P5-2's *design* (not merged code), so P5-6 cannot be dispatched until P5-2 is merged to `main` — its Step 0 makes Codex re-verify every P5-2 symbol it relies on against the real merged code first.
- **P5-4 can run in parallel with P5-2.** P5-2's own coordination note (D-P5-2-T) says: "P5-4 (may run in parallel): it owns `question_engine.py`, the questionnaire routes/templates and `assessment_detail`'s questionnaire-tab data. This task's `assessment_detail` edits are limited to `framework_display`, `gap_items` removal and `active_framework_finding_count`. If P5-4 lands first, rebase and keep both sides." Use two separate worktrees as usual; expect at most a mechanical rebase in `assessment_detail`, not a real conflict.
- **Order:** dispatch P5-2 and P5-4 together (separate worktrees, separate Codex processes). Merge P5-2 first if both finish around the same time (P5-6 is waiting on it), rebase P5-4 onto the new `main` if it hasn't merged yet, then dispatch P5-6 only after P5-2 is on `main`.

## Constraints

Same mechanics as every prior phase in this project — restated because P5-2 has one extra requirement:

- **Model:** dispatch Codex with `gpt-5.6-luna` at `xhigh` reasoning effort (`gpt-5.6-sol` burned the account's quota twice in Phase 3 at medium effort).
  ```
  codex exec -m gpt-5.6-luna -c model_reasoning_effort="xhigh" -s workspace-write \
    -C <worktree-path> --skip-git-repo-check "$(cat <prompt-file>)"
  ```
- **One worktree per task, never shared.** `git worktree add ../dpdpa-gap-tool-p5-N -b codex/p5-N-<slug> main` (or, for P5-6, based off `main` **after** P5-2 merges), symlink `.venv`, copy `.env`. Verify the baseline suite passes in each fresh worktree before dispatching (expect the known pre-existing teardown error in `tests/test_workpaper.py::test_smoke_full_assessment_traceability`, and possibly the documented intermittent `test_longitudinal_demo.py::test_scenario_9_rollups_and_integrated_reporting` ordering flake — both are called out as non-regressions in every merged Phase 5 handoff's own Results).
- **Codex cannot commit.** Its sandbox refuses `.git` writes. Commit on its behalf after verifying its diff, as always.
- **No Claude attribution anywhere in these three tasks' commits or PRs** — no `Co-Authored-By: Claude` trailer, no "Generated with Claude Code" footer. This has held since PR #31 and every merged Phase 5 PR (#38-#41) follows it; keep doing so. (This constraint does not apply to this handoff file itself, which is a process/meta document written by the cloud session under the ordinary attribution rule for its own commits.)
- **Verify, don't trust.** `git diff --stat` against every file each handoff said not to touch (should be empty except the pre-declared shared regions), and re-run `.venv/bin/pytest -q` yourself independently.
- **Independent Sonnet adversarial review required before any PR opens**, briefed to trace every stated invariant against the real diff, run the suite independently, and try to break it. Tell it what NOT to flag (pre-existing style, deferred features, missing auth by design).
- **P5-2 gets a second, more skeptical review pass**, explicitly, per the plan and per D-P5-2's own header. Model it on how P4-4's second pass worked (see `tasks/handoffs/2026-09-24-phase-4-dispatch-continuation.md`'s `## Results` "What surprised me" section for the shape a genuinely adversarial second pass took there: live concurrency stress, an attempted attack, hand-tampered state — not a second read-through). For P5-2, the equivalent stress points are: (1) prove the release manifest actually invalidates on every path the handoff claims (reopen, re-decide, re-run, scope edit) and does *not* spuriously invalidate on a `proposal_withheld` no-op re-run; (2) prove an issued snapshot's bytes are unchanged across every one of those operations; (3) try to find a release surface that still reads `GapItem`/`review_status` for a decision (the handoff's own D-P5-2-R grep guards are a starting point, not a substitute for tracing); (4) check the three legacy-data cases in D-P5-2-Q actually fail closed against a hand-constructed legacy assessment, not just against the new test file's fixtures.
- **Alembic head coordination.** P5-3 shipped head `8b2d5f7e1c34`. Check whether P5-2's implementation adds a new revision (its handoff doesn't plan one, but verify against what actually lands) before branching P5-6, since P5-6's own handoff explicitly defers to "whatever is actually head" at its dispatch time.
- **Only after review comes back clean:** push and open the PR. No attribution footer. State in the PR description exactly what was verified (test counts, invariants traced, protected-file zero-diffs, and for P5-2 specifically the second review's findings), matching the detail level of PRs #38-#41.
- **Rebase coordination:** if P5-2 and P5-4 land PRs concurrently, merge P5-2 first (it gates P5-6), then rebase P5-4 onto the new `main` before merging it. Conflicts should fall inside the pre-declared shared region in `assessment_detail`.
- **Don't relitigate numbered decisions** (`D-P5-2-*`, `D-P5-4-*`, `D-P5-6-*`). If the actual code forces a deviation, stop and report it in that handoff's own `## Results` section rather than picking an alternative.

## Verification

For each task, before considering it done:
1. `git diff --stat main` in the task's worktree matches the handoff's declared file scope (plus pre-declared shared files).
2. `.venv/bin/pytest -q` passes at baseline plus the task's new tests, re-run yourself.
3. Task-specific checks from each handoff's own `## Test scenarios` / `## Done criteria` (P5-2: the release manifest/staleness behavior, the three legacy-data paths, the deterministic summary text, the PDF's additive-only diff; P5-4: the cluster-status rule's three branches, the pre-fill commit-on-update fix, the screening copy fix; P5-6: the release-gate-independence guard test, the magic-link v2 scope_json shape, byte-identical reissue behavior for unchanged sources).
4. Independent Sonnet review comes back clean (P5-2: **two** clean reviews, not one).
5. After all three are merged: full suite green on `main`, `tasks/todo.md` Phase 5 section fully checked off except P5-7 (which stays reserved/unscheduled per the plan), `CLAUDE.md` "Current phase" line updated to "Phase 5 complete", project auto-memory updated.

## Report back

Append a `## Results` section to **this file** summarizing: which of the three tasks merged (with PR links), any deviations from the handoffs and why, the final full-suite test count, what P5-2's second review pass actually found (even if nothing — say so explicitly and describe what it stress-tested), and anything that surprised you.
